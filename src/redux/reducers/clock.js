// import { SET_SELECTED_TODO } from '../action';

const initialState = {
	minutes: 0,
	seconds: 30
}

function clock(state = initialState, action) {
	switch (action.type) {
		default:
			return state;
	}
}

export default clock